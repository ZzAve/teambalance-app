name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: teambalance
          POSTGRES_PASSWORD: teambalance
          POSTGRES_DB: teambalance
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U teambalance"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd="redis-cli ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: app/package-lock.json

      - uses: gradle/actions/setup-gradle@v4

      - run: make ci
      - run: make lint

      # Needed before `make test`: the Storybook stories run in a headless-chromium
      # Vitest browser project, and the real e2e suite also drives Chromium.
      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium
        working-directory: app

      - run: make test

      # Real full-stack e2e last (slowest layer): backend via bootRun (e2e profile) against the
      # workflow's Postgres/Redis service containers, health-gated by scripts/e2e.sh.
      - name: Real full-stack e2e
        run: make e2e

      - name: Upload backend e2e log
        if: ${{ failure() }}
        uses: actions/upload-artifact@v4
        with:
          name: backend-e2e-log
          path: /tmp/teambalance-e2e-backend.log
          retention-days: 7

      - name: Upload Playwright report
        if: ${{ failure() }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            app/playwright-report/
            app/test-results/
          retention-days: 7

