// design-tokens is a static asset module — no compilation needed.
// Consumed directly by app/ (tokens.css imported from app/src/app/styles/global.css) and
// www/ (tokens.css). The old tailwind-preset.js this comment used to mention was deleted in #338 —
// nothing imported it.
